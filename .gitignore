.vscode
# Build / install / cache (colcon, clangd)
build/
install/
log/
.cache/

# Trial result data (large; per-trial timestamped subdirs)
tools/perceptive_dev_v2/results/

# LaTeX build outputs (keep .tex source, drop generated)
note/*.aux
note/*.fdb_latexmk
note/*.fls
note/*.log
note/*.out
note/*.synctex.gz
note/*.toc

# Note directory entirely untracked (per user request)
note/
