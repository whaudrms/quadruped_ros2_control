# Contact Timing Bias Handover Artifacts

This directory stores the validated handover outputs that were originally generated under `/home/ho/ros2_ws/filtered_results/`.

Included contents:

- filtered run tables,
- exclusion logs,
- researcher summary tables,
- validation report and sample rows,
- compact handover tables,
- final figures,
- figure captions,
- scene XML snapshots used for the study.

These files are copied here so that the GitHub sharing branch contains both the implementation and the final validated package in one place.
